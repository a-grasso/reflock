{
  "description": "BUG-07: check --format json puts a ScopeError on stdout as {\"error\": ...}, not stderr",
  "steps": [
    {
      "cmd": "check",
      "args": ["--format", "json", "nope.md"],
      "expect_exit": 2,
      "expect_json": {"error": "no such path in tree: nope.md"},
      "expect_stderr_empty": true
    }
  ]
}
