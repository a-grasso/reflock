{
  "description": "UX-03: an UNSTAMPED finding gets both the explain and the stamp next-step hints",
  "steps": [
    {
      "cmd": "check",
      "expect_exit": 1,
      "expect_contains": [
        "Run `reflock explain <file>:<line>` for details on any of the above.",
        "Run `reflock stamp` to fill in UNSTAMPED pins."
      ]
    }
  ]
}
