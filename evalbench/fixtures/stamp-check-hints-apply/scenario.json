{
  "description": "UX-03: stamp --check with a pending pin gets a next-step hint",
  "steps": [
    {
      "cmd": "stamp",
      "args": ["--check"],
      "expect_exit": 1,
      "expect_contains": ["Run `reflock stamp` to apply."]
    }
  ]
}
