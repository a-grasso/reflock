{
  "description": "BUG-07 + AGT-01: backlinks --format json puts an unknown-path error on stdout inside the envelope",
  "steps": [
    {
      "cmd": "backlinks",
      "args": [
        "nope.md",
        "--format",
        "json"
      ],
      "expect_exit": 2,
      "expect_stderr_empty": true,
      "expect_json_subset": {
        "findings": [],
        "error": {
          "message": "no such file in index: nope.md",
          "kind": "scope"
        }
      }
    }
  ]
}
