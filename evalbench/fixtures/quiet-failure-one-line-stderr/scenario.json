{
  "description": "check -q with one DANGLING finding: stdout stays empty (the summary line goes to stderr only, which this harness cannot assert directly - see NOTE), nonzero exit.",
  "steps": [
    {
      "cmd": "check",
      "args": ["-q"],
      "expect_exit": 1,
      "expect_not_contains": [
        "DANGLING",
        "problem(s)",
        "reflock:"
      ]
    }
  ]
}
