{
  "description": "`stamp --check` on an opted-in but unstamped pin exits nonzero and names it in the output.",
  "steps": [
    {
      "cmd": "stamp",
      "args": ["--check"],
      "expect_exit": 1,
      "expect_contains": ["a.md:1", "t.md#decision", "unstamped"]
    }
  ]
}
