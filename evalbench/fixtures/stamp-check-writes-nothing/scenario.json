{
  "description": "`stamp --check` on a dirty-in-reflock-terms tree writes nothing, then real `stamp` makes exactly the reported edit. NOTE: run_bench.py has no mechanism to assert byte-for-byte file identity across steps (no expect_file_bytes / mtime comparison) - approximated here by asserting the pin marker is still empty after --check, then filled after a real stamp.",
  "steps": [
    {
      "cmd": "stamp",
      "args": ["--check"],
      "expect_exit": 1,
      "expect_contains": ["a.md:1", "unstamped"],
      "expect_file_regex": {
        "a.md": "<!--@-->"
      }
    },
    {
      "cmd": "stamp",
      "args": [],
      "expect_exit": 0,
      "expect_file_regex": {
        "a.md": "<!--@[0-9a-f]{8}-->"
      }
    }
  ]
}
