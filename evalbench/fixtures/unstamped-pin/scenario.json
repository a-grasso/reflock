{
  "description": "Opting into a pin (`<!--@-->`) without stamping is UNSTAMPED.",
  "steps": [
    {
      "cmd": "check",
      "args": [
        "--json"
      ],
      "expect_exit": 1,
      "expect_json": [
        {
          "verdict": "UNSTAMPED",
          "file": "a.md",
          "line": 1,
          "target": "t.md#decision",
          "detail": "run: reflock stamp"
        }
      ]
    }
  ]
}
