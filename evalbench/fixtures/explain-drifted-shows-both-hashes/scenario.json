{
  "description": "a DRIFTED reference shows pin and current, and states the prior text is unrecoverable",
  "steps": [
    {
      "cmd": "stamp",
      "args": [],
      "expect_exit": 0
    },
    {
      "cmd": "explain",
      "args": ["a.md:1"],
      "write": {
        "t.md": "# H\n\n## Decision\n\nWe chose Y instead.\n"
      },
      "expect_exit": 1,
      "expect_contains": ["verdict     DRIFTED", "pin", "current", "not recoverable"]
    }
  ]
}
